#include "cryptonote_config.h"
#include <iostream>
int main(int argc, char** argv) {
    // Monero’dan aldığı main() kodunu stub’ladık:
    // Başlatıcı: Palladium Corp, daemon adı: stashusd
    std::cout << "Starting Stash USD daemon (Monero fork)…\n";
    // Burada Monero’nun daemon logic’i devreye girer
    return 0;
}
